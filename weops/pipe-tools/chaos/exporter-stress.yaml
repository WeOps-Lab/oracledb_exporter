kind: Schedule
apiVersion: chaos-mesh.org/v1alpha1
metadata:
  namespace: oracle
  name: oracle-exporter-stress
  annotations:
    experiment.chaos-mesh.org/pause: 'false'
spec:
  schedule: '*/3 * * * *'
  startingDeadlineSeconds: null
  concurrencyPolicy: Allow
  historyLimit: 1
  type: StressChaos
  stressChaos:
    selector:
      namespaces:
        - oracle
      labelSelectors:
        'pod_type': 'exporter'
    mode: all
    stressors:
      memory:
        workers: 2
        size: 20%
      cpu:
        workers: 2
        load: 20
    duration: 20s
